info:
  name: Logout
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/v1/logout"
  body:
    type: json
    data: |-
      {
        "refresh_token": "{{auth_token}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5