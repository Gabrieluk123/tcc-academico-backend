info:
  name: /v1/users - List
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/v1/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
