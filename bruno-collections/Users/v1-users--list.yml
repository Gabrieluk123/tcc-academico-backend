info:
  name: List Users
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/v1/users?details=role&include=role"
  params:
    - name: details
      value: role
      type: query
    - name: include
      value: role
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
